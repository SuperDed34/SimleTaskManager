import { Box } from "@mui/material"

const NoTasks = () => {
  return (
    <Box sx={{
      w: '100%',
      h: '100%',
      textAlign: 'center',
      mt: '15%'
    }}>No tasks here...</Box>
    )
}

export default NoTasks